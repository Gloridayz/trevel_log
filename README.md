# trevel_log

가족이 함께 쓰는 여행 기록/준비 웹앱. 로그인 없이 PIN으로 입장하는 정적 사이트입니다. (`index.html` 단일 파일, 빌드 도구 없음)

## 화면 구조

PIN 입장 → **카테고리(여행) 선택** 화면 → 카테고리 하나를 고르면 그 안에서 탭 5개를 씁니다. 카테고리는 관리자만 추가/이름변경/삭제할 수 있고, 카테고리마다 "기본 통화"를 지정할 수 있습니다 (예: "26년 대만여행" → TWD, "일상" → KRW). 지정한 기본 통화는 그 카테고리의 경비 입력 폼에 자동으로 선택됩니다.

1. **추억** — 기록 탭에 쌓인 내용을 보기 전용으로 열람 (누구나 조회 가능, 작성 폼 없음). 카테고리 전체 또는 기록 1건 단위로 "📄 PDF" / "🖼️ 이미지"로 내보낼 수 있습니다 (원본 사진 화질 그대로 사용). 관리자는 카드에서 바로 "수정"할 수 있습니다 (7일 제한 없음, 사진도 개별 삭제/추가 가능).
2. **기록** — 작성 전용 화면 (관리자만 작성 가능, 폼만 노출)
3. **위시리스트·숙소** — 한 곳씩 입력 또는 여러 곳 한번에 붙여넣기, 구글맵/장소명으로 자동 채우기 지원. 카드에서 "다녀옴"을 체크하면 확인창 후 그 장소가 기록으로 자동 옮겨지고 위시리스트에서는 사라집니다 (원본 row는 지우지 않고 `converted` 표시만 함).
4. **경비** — 날짜별 입력, 항목은 "항목 관리" 화면에서 추가·수정·삭제하는 드롭다운(재사용 가능한 이름표) + 건별 상세 메모. 카드 보기/표 보기 전환, 표에서는 헤더 클릭으로 정렬하고 날짜·항목·통화로 필터링할 수 있으며, CSV로 내보낼 수 있습니다.
5. **준비물** — 담당(챙기는 사람)을 관리형 목록(`checklist_owners`)에서 칩으로 선택 — "⚙ 담당자 관리"에서 이름/색상 추가·수정·삭제. 진행률(N/M 완료, %)과 "남은 것만 보기" 필터.

## 처음 설정할 때

### 1. Supabase 테이블 만들기

- **완전히 새로 시작하는 경우**: Supabase 프로젝트의 **SQL Editor**에서 `supabase/schema.sql` 내용을 그대로 실행하세요.
- **예전 버전(카테고리가 자유 텍스트였던 버전)을 이미 쓰고 있던 경우**: 아래 순서대로 실행하세요.
  1. `supabase/migrate_002_trip_categories.sql` — 기존 기록/위시리스트/경비/준비물 데이터를 지우지 않고 새 구조(카테고리 선택 화면)로 옮겨줍니다.
  2. `supabase/migrate_003_default_currency.sql` — 카테고리별 기본 통화 컬럼을 추가합니다.
  3. `supabase/migrate_004_checklist_owners.sql` — 준비물 담당자를 자유 텍스트에서 관리형 목록으로 전환합니다.
  4. `supabase/migrate_005_wishlist_converted.sql` — 위시리스트 "다녀옴" 기능에 필요한 컬럼을 추가합니다.

  모두 다시 실행해도 안전합니다 (이미 적용됐으면 건너뜁니다). 실행 전 Supabase 대시보드의 백업 상태를 한 번 확인해두는 걸 권장합니다.

### 2. 위시리스트 "자동 채우기" 기능 — Google Places API 연결

이 기능은 `index.html`이 아니라 `/api/place.js`라는 Vercel 서버리스 함수를 통해 동작합니다 (API 키를 브라우저에 노출하지 않기 위함).

1. Google Cloud Console에서 **Places API (New)**를 활성화하고 API 키를 발급받으세요.
2. Vercel 프로젝트의 **Settings → Environment Variables**에 아래 값을 추가하세요.

   | 변수 | 값 |
   | --- | --- |
   | `GOOGLE_PLACES_API_KEY` | 발급받은 Places API 키 |

3. 환경변수를 추가한 뒤에는 Vercel에서 **Redeploy**를 한 번 해줘야 적용됩니다.
4. 위시리스트 탭 → "한 곳씩 입력" → 위치 검색어를 넣고 "✨ 자동 채우기"를 누르면 장소명/특징/사진이 채워집니다. 저장 전에 자유롭게 수정할 수 있습니다.

이 API 키는 `/api/place.js` 안에서만 서버 쪽에서 사용되고, `index.html`에는 절대 들어가지 않습니다.

## 배포 흐름

1. `index.html` (또는 `api/place.js`) 수정
2. 이 저장소에 커밋/푸시
3. Vercel이 자동으로 재배포 (별도 조작 불필요)
